# 桌面宠物 - 问题已修复

## 修复的问题

1. ✅ **窗口位置错误** - 之前在屏幕外（负坐标），现在在屏幕右侧中间
2. ✅ **尺寸太小** - 从 48x48 放大到 96x96，更容易看到
3. ✅ **透明度问题** - macOS 上使用 gray95 背景而不是透明，避免显示问题
4. ✅ **端口占用** - 添加异常处理，允许多个实例

## 当前状态

桌宠现在会显示在：
- **位置**：屏幕右侧中间
- **尺寸**：96x96 像素
- **背景**：浅灰色（macOS）或透明（Windows）

## 使用方法

### 从 GA 启动
```bash
python3 launch.pyw
```
然后点击侧边栏的 **"🐱 桌面宠物"** 按钮

### 独立启动
```bash
python3 frontends/desktop_pet_v2.pyw
```

## 交互方式

- **单击拖动** - 移动桌宠位置
- **双击** - 关闭桌宠
- **右键菜单**：
  - Animation - 切换动画（idle/walk/run/sprint）
  - Switch Skin - 切换皮肤（8 种可选）
  - Exit - 退出

## 测试命令

```bash
# 启动桌宠
python3 frontends/desktop_pet_v2.pyw &

# 发送消息
curl "http://127.0.0.1:51983/?msg=Hello"

# 切换状态
curl "http://127.0.0.1:51983/?state=run"

# 关闭桌宠
pkill -9 -f desktop_pet
```

## 调整尺寸

如果你觉得 96x96 太大或太小，可以编辑：
```bash
frontends/skins/vita/skin.json
```

修改 `size` 字段：
```json
"size": { "width": 128, "height": 128 }
```

推荐尺寸：
- 小：64x64
- 中：96x96（当前）
- 大：128x128
- 超大：192x192

## 故障排查

### 看不到桌宠
```bash
# 检查进程
ps aux | grep desktop_pet

# 检查窗口位置
python3 -c "
import Quartz
windows = Quartz.CGWindowListCopyWindowInfo(
    Quartz.kCGWindowListOptionOnScreenOnly,
    Quartz.kCGNullWindowID
)
for window in windows:
    owner = window.get('kCGWindowOwnerName', '')
    if 'python' in owner.lower():
        bounds = window.get('kCGWindowBounds', {})
        print(f'位置: ({bounds.get(\"X\")}, {bounds.get(\"Y\")})')
        print(f'尺寸: {bounds.get(\"Width\")}x{bounds.get(\"Height\")}')
"
```

### 端口被占用
```bash
# 清理旧进程
pkill -9 -f desktop_pet

# 检查端口
lsof -i :51983
```

## 下一步

桌宠系统已经完全可用，你可以：
1. 从 GA 启动并测试
2. 调整尺寸到你喜欢的大小
3. 尝试切换不同的皮肤
4. 添加更多皮肤资源
