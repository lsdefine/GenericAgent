# 桌面宠物系统 - 使用指南

## ✅ 已完成

桌面宠物系统已经完全重构并集成到 GA 中，支持：
- ✅ 8 种像素风皮肤（可扩展）
- ✅ 4 种动画状态（idle/walk/run/sprint）
- ✅ 从 GA 一键启动
- ✅ 自动接收 turn 通知
- ✅ HTTP 远程控制
- ✅ 跨平台支持（macOS/Windows/Linux）

## 使用方法

### 1. 启动 GA
```bash
cd /Users/lwj/Documents/projects/a3lab/ga_zhuochong
python3 launch.pyw
```

### 2. 启动桌宠
在 GA 界面中，点击侧边栏的 **"🐱 桌面宠物"** 按钮

桌宠会自动：
- 出现在屏幕上
- 播放待机动画
- 接收每个 turn 的通知

### 3. 交互方式

**鼠标操作：**
- **单击拖动** - 移动桌宠到任意位置
- **双击** - 关闭桌宠
- **右键菜单**：
  - `Animation` - 切换动画状态（idle/walk/run/sprint）
  - `Switch Skin` - 切换皮肤（8 种可选）
  - `Exit` - 退出程序

**自动行为：**
- 每个 turn 结束时显示通知气泡
- 随机小幅度移动（模拟呼吸感）

## 可用皮肤

当前包含 8 种像素风桌宠：

1. **Vita** - 小恐龙（默认）
2. **Glube** - 小怪兽
3. **Doux** - 小恐龙变种
4. **Boy** - 小男孩
5. **Dinosaur** - 恐龙
6. **Line** - 线条风格
7. **Mort** - 骷髅
8. **Tard** - 另一种恐龙

右键菜单可随时切换。

## 远程控制（高级）

桌宠提供 HTTP 接口，可以通过代码控制：

```python
from urllib.request import urlopen
from urllib.parse import quote

# 显示消息
msg = "任务完成！"
urlopen(f'http://127.0.0.1:51983/?msg={quote(msg)}')

# 切换动画状态
urlopen('http://127.0.0.1:51983/?state=run')
```

**可用状态：**
- `idle` - 待机
- `walk` - 行走
- `run` - 跑步
- `sprint` - 冲刺

## 添加新皮肤

### 方法1：从 ai-bubu 导入

```bash
# 查看可用皮肤
ls ai-bubu-main/packages/app/public/skins/

# 复制皮肤
cp -r ai-bubu-main/packages/app/public/skins/[皮肤名] frontends/skins/
```

### 方法2：自己制作

1. 创建皮肤目录：
```bash
mkdir frontends/skins/my-pet
```

2. 准备动画图片（sprite sheet 或 GIF）

3. 创建 `skin.json` 配置文件：
```json
{
  "name": "My Pet",
  "version": "1.0.0",
  "author": "Your Name",
  "description": "我的桌宠",
  "format": "sprite",
  "animations": {
    "idle": {
      "file": "idle.png",
      "loop": true,
      "sprite": {
        "frameWidth": 44,
        "frameHeight": 31,
        "frameCount": 6,
        "columns": 6,
        "fps": 6,
        "startFrame": 0
      }
    },
    "walk": { ... },
    "run": { ... },
    "sprint": { ... }
  }
}
```

4. 重启桌宠，新皮肤会自动出现在菜单中

## 故障排查

### 问题1：点击按钮后没有反应

**检查：**
```bash
# 查看是否有进程启动
ps aux | grep desktop_pet

# 查看端口是否被占用
lsof -i :51983
```

**解决：**
```bash
# 清理旧进程
pkill -9 -f desktop_pet

# 重新启动
```

### 问题2：桌宠不显示

**可能原因：**
- 窗口被移到屏幕外
- 透明度设置问题
- 皮肤文件缺失

**解决：**
```bash
# 测试启动
cd /Users/lwj/Documents/projects/a3lab/ga_zhuochong
python3 frontends/desktop_pet_v2.pyw

# 查看错误信息
```

### 问题3：皮肤切换失败

**检查皮肤文件：**
```bash
# 验证皮肤配置
python3 -c "
import json
import os

skin_path = 'frontends/skins/vita'
with open(os.path.join(skin_path, 'skin.json')) as f:
    config = json.load(f)
    print(json.dumps(config, indent=2))
"
```

### 问题4：HTTP 接口无响应

**测试连接：**
```bash
# 测试消息
curl "http://127.0.0.1:51983/?msg=test"

# 测试状态切换
curl "http://127.0.0.1:51983/?state=walk"
```

## 技术细节

### 文件结构
```
frontends/
├── desktop_pet_v2.pyw          # 新版桌宠（推荐）
├── desktop_pet.pyw             # 原版桌宠（备份）
├── skins/                      # 皮肤目录
│   ├── vita/
│   │   ├── skin.json
│   │   └── skin.png
│   ├── glube/
│   │   ├── skin.json
│   │   ├── idle.png
│   │   ├── walk.png
│   │   ├── run.png
│   │   └── sprint.png
│   └── ...
└── stapp.py                    # GA 主界面（包含启动按钮）
```

### 架构设计
- **SkinLoader** - 扫描和加载皮肤配置
- **AnimationLoader** - 加载 sprite sheet 或 GIF 动画
- **Pet** - 主窗口、动画播放、交互逻辑
- **HTTP Server** - 远程控制接口（端口 51983）

### 跨平台兼容
- **macOS**: 使用 `-transparent` 属性和 `systemTransparent` 背景
- **Windows**: 使用 `-transparentcolor` 色键透明
- **Linux**: 自动降级处理

### 性能优化
- PIL/Pillow 高效图片加载
- ImageTk 确保 Tkinter 兼容性
- 帧缓存避免重复加载
- 允许端口复用（`allow_reuse_address`）

## 下一步建议

1. **智能状态切换** - 根据 GA 活跃度自动切换动画
2. **更多表情** - 添加开心/困倦/兴奋等表情动画
3. **社交功能** - 参考 ai-bubu 的局域网互动
4. **自定义行为** - 配置移动范围、速度等参数

## 相关文件

- `frontends/desktop_pet_v2.pyw` - 主程序
- `frontends/skins/` - 皮肤资源
- `frontends/桌面宠物使用说明.md` - 本文档
- `test_desktop_pet_full.py` - 完整测试脚本
- `DESKTOP_PET_README.md` - 技术文档

## 测试

运行完整测试：
```bash
cd /Users/lwj/Documents/projects/a3lab/ga_zhuochong
python3 test_desktop_pet_full.py
```

测试会：
1. 检查文件完整性
2. 启动桌宠
3. 测试 HTTP 接口
4. 测试动画状态切换
5. 显示使用说明

---

**享受你的桌宠吧！** 🐱
