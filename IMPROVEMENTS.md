Improvements suggested by @sharkello

Summary
-------
Targeted changes to improve onboarding, examples, and contributor friction for GenericAgent.

Top items
---------
- Add a short TL;DR (3 lines) at top of README with a one‑command quickstart.
- Add `examples/python-hello.md` showing minimal install + run.
- Provide a Docker quickstart (optional) or a single script to bootstrap the environment.
- Add CONTRIBUTING.md and a short CODE_OF_CONDUCT.md.
- Create 5 good‑first‑issue tickets (docs, tests, small features).
- Add CI (GitHub Actions) and insert a status badge in README.

Suggested README TL;DR
----------------------
```markdown
# GenericAgent
A minimal self‑evolving autonomous agent framework. Try the demo in 3 steps.
Quickstart: `pip install -r requirements.txt && python launch.pyw`
```

Good first issues to add
-----------------------
- "Add examples/python-hello.md" — `good first issue`, `examples`.
- "Create CONTRIBUTING.md" — `good first issue`, `docs`.
- "Add GitHub Actions workflow + badge" — `good first issue`, `ci`.
- "Add unit test for a pure function" — `good first issue`, `test`.
- "Add Windows build notes to README" — `good first issue`, `docs`.

Notes
-----
I created a local branch with this file and committed it. I can prepare PR text and patches for maintainers when you want.
