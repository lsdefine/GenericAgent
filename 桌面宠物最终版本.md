# 桌面宠物 - 最终版本

## 已修复的问题

1. ✅ **透明背景** - macOS 使用 `-transparent` 和 `systemTransparent`
2. ✅ **关闭 GA 自动清理** - launch.pyw 退出时会自动 kill 桌宠进程
3. ✅ **移除自动移动** - 桌宠不再随机移动，只播放动画

## 当前功能

### 动画状态
- **idle** - 待机动画（原地跳动）
- **walk** - 行走动画
- **run** - 跑步动画
- **sprint** - 冲刺动画

### 交互方式
- **单击拖动** - 手动移动桌宠位置
- **双击** - 关闭桌宠
- **右键菜单**：
  - Animation - 切换动画状态
  - Switch Skin - 切换皮肤（8 种）
  - Exit - 退出

### HTTP 控制
```bash
# 切换动画状态
curl "http://127.0.0.1:51983/?state=walk"
curl "http://127.0.0.1:51983/?state=run"
curl "http://127.0.0.1:51983/?state=sprint"
curl "http://127.0.0.1:51983/?state=idle"

# 显示消息气泡
curl "http://127.0.0.1:51983/?msg=Hello"
```

## 使用方法

### 从 GA 启动
```bash
python3 launch.pyw
```
点击侧边栏 "🐱 桌面宠物" 按钮

**重要**：关闭 GA 时，桌宠会自动关闭

### 独立启动
```bash
python3 frontends/desktop_pet_v2.pyw
```

## 技术细节

### 透明背景实现
- **macOS**: 使用 `-transparent` 属性 + `systemTransparent` 背景
- **Windows**: 使用 `-transparentcolor` 色键透明
- **Linux**: 自动降级处理

### 生命周期管理
- launch.pyw 使用 `atexit.register()` 注册清理函数
- 退出时执行 `pkill -9 -f desktop_pet`（macOS/Linux）
- Windows 使用 `taskkill` 命令

### 动画系统
- 基于 sprite sheet 的帧动画
- 支持不同 FPS 设置
- 自动缩放到显示尺寸

## 可用皮肤

当前包含 8 种像素风桌宠：
1. Vita - 小恐龙（默认，96x96）
2. Glube - 小怪兽
3. Doux - 小恐龙变种
4. Boy - 小男孩
5. Dinosaur - 恐龙
6. Line - 线条风格
7. Mort - 骷髅
8. Tard - 另一种恐龙

## 调整尺寸

编辑 `frontends/skins/vita/skin.json`：
```json
"size": { "width": 128, "height": 128 }
```

推荐尺寸：
- 小：64x64
- 中：96x96（当前）
- 大：128x128
- 超大：192x192

## 故障排查

### 看不到桌宠
```bash
# 检查进程
ps aux | grep desktop_pet

# 检查窗口
python3 -c "
import Quartz
windows = Quartz.CGWindowListCopyWindowInfo(
    Quartz.kCGWindowListOptionOnScreenOnly,
    Quartz.kCGNullWindowID
)
for w in windows:
    if 'python' in w.get('kCGWindowOwnerName', '').lower():
        print(w.get('kCGWindowBounds'))
"
```

### 背景不透明
- 确认使用的是 desktop_pet_v2.pyw
- macOS 需要系统支持窗口合成
- 可能需要重启桌宠

### 关闭 GA 后桌宠仍在
```bash
# 手动清理
pkill -9 -f desktop_pet
```

## 与 GA 集成

桌宠会在每个 turn 结束时收到通知：
- 显示 turn 编号
- 显示任务摘要
- 显示完成状态

这些通知会以气泡形式显示在桌宠旁边，3 秒后自动消失。

## 下一步建议

1. **智能状态切换** - 根据 GA 活跃度自动切换动画
2. **更多皮肤** - 从 ai-bubu 或 itch.io 导入
3. **自定义行为** - 配置动画播放规则
4. **音效支持** - 添加动作音效
